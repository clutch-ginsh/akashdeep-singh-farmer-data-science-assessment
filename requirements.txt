# Pinned dependencies for reproducibility (Python >= 3.10).
# Preferred install is `pip install .` (or `pip install -e ".[dev]"`), which
# reads pyproject.toml. This file is provided as an explicit, pinned fallback
# matching a verified working environment.

# --- core / runtime ---
numpy==2.5.2
pandas==3.0.5
scipy==1.18.1
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
torch==2.13.0

# --- development (tests + notebook) ---
pytest==9.1.1
jupyter==1.1.1
ipykernel==7.3.0
